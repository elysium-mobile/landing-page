---
/**
 * FeatureList — bare `<ul>` wrapper, gap + bottom margin only.
 *
 * Items are FeatureItem components; this exists so the value-block markup
 * stays tidy without inline `<ul class="...">` repetition.
 */
---

<ul class="list-none p-0 m-0 mb-7 flex flex-col gap-3.5">
  <slot />
</ul>
